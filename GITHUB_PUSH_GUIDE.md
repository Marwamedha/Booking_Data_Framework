# GitHub Push Guide

Use this guide to upload the repository contents to GitHub.

```bash
cd /workspaces/Booking_Data_Framework
git add .
git commit -m "Initial commit: Complete Uber Rides Analytics Framework"
git push origin main
```

If GitHub authentication fails, use a personal access token or GitHub Codespace authentication.
